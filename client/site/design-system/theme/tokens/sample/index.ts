// NOTE: This is here to show an example of how to add more token sets in the future
import { TokenVariant } from "../../../types/shared";
import colors from "./colors";
const Sample: TokenVariant = {
  colors,
};

export default Sample;
